{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@lunajs/monorepo",
  "packageManager": "pnpm@9.3.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "turbo": "^2.0.3",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
